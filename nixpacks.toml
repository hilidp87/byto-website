[variables]
NODE_ENV = "production"

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node -e \"console.log('DATABASE_URL:', process.env.DATABASE_URL ? process.env.DATABASE_URL.replace(/:([^:@]+)@/, ':***@') : 'NOT SET')\" && npx prisma db push && next start -H 0.0.0.0 -p ${PORT:-3000}"
